# Deep Learning RAG Assistant

AI learning assistant for Deep Learning courses.

## Quick Start
1. Install: `pip install -r requirements.txt`
2. Add PDFs to `course_materials/`
3. Run: `streamlit run src/app.py`
